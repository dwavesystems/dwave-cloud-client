---
features:
  - |
    Add ``dwave.cloud.testing.mocks.hybrid_cqm_solver_data()``, a mock metadata
    generator for the CQM hybrid solver.
fixes:
  - |
    Update ``dwave.cloud.testing.mocks.hybrid_*_solver_data()`` generators to
    include the full list of solver properties (albeit with mock/dummy values).
