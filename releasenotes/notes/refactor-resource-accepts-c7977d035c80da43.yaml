---
features:
  - |
    Simplify ``dwave.cloud.api.resource.accepts`` interface (require only
    ``media_type`` and ``version``), but retain flexibility (pass all keyword
    arguments to ``VersionedAPISession.set_accept``).
