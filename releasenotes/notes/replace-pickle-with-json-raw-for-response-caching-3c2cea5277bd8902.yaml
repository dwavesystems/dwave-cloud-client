---
features:
  - |
    Make ``CachingSessionMixin`` safer by replacing DiskCache's default ``pickle``
    serialization with JSON (for response headers) + raw binary (for response
    content).
  - |
    Use JSON serialization for auth credentials (instead of DiskCache's default
    ``pickle``).
