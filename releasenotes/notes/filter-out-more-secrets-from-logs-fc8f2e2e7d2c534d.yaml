---
features:
  - |
    Filter out more formats of API token-like strings from logs. In addition to the
    Solver API tokens, we now filter generic 32-char+ hex tokens (Leap API access
    token format), and UUID tokens (all variants).
    See `#551 <https://github.com/dwavesystems/dwave-cloud-client/issues/551>`_.
