---
features:
  - |
    Filter out API token-like strings from logs. Most notably, we make sure to
    redact Solver API tokens, but will filter out any prefixed hex token.
    See `#391 <https://github.com/dwavesystems/dwave-cloud-client/issues/391>`_.
